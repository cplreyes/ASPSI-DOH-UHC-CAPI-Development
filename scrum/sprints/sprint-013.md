---
sprint: 013
start: 2026-07-06
end: 2026-07-10
status: closed
sprint_length: 1 week (5 working days)
deliverable_anchor: F2 configuration-setup migration to our prod server (also backs F1/F3/F4 CSWeb) · Supervisor App finalization (+ UAT Round 6) · stay pretest-ready · ETL extraction DEFERRED; tabulation clarifications → 3pm meeting · open: questionnaire-change timing
created: 2026-07-03 — Mode D skeleton at the S012→S013 boundary
locked: 2026-07-06 (Mode A — Monday kickoff); RE-ALIGNED 2026-07-06 to the Monday meeting notes
closed: 2026-07-20 — retro filled 10 days LATE (Day 15 of 5). The window lapsed because the PRETEST WENT LIVE 2026-07-15 and reactive field support took the lane (as Goal B designed) — but nobody closed or rolled the sprint container. Closed Mode D from the record (log.md + backlog + git + deploy history).
---

# Sprint 013 — F2 to prod server + Supervisor App finalization (pretest-ready) — CLOSED 2026-07-20 (LATE)

> [!info] Where S012 left the project
> **Goal landed — first sprint where plan and value agreed.** All four perennial carries cleared by Day 2 (E0-SCRUM-SYNC finally BUILT; PSA closed out-of-lane; Goal B killed; field-ready criterion wired into `check_field_ready.py` + verified FIELD-READY). UAT R5 closed 06-29 (78/79). The freeze week became a build week: pretest assignments ready (Los Baños, 84 QNs, printed sheets), tabulation plan spec-complete + Stata-12 proven (197 SSRCS tables), fleet version-stamped v1.0.x on all 4 surfaces, DHS benchmark → data layer + R1a + R2 + R3 shipped byte-identical (freeze intact). Full record: `scrum/sprints/sprint-012.md` + `log.md`.

## Sprint Goal

> **Migrate the F2 configuration setup onto our own production server (which also backs the F1/F3/F4 CSWeb sync) and finalize the Supervisor App (through UAT Round 6) — while staying pretest-ready.** ETL variable extraction (`transform.py`) is **deferred**; the tabulation clarifications go to Ms. Myra's **3pm tabulation meeting today** (Carl shares the findings). _Re-aligned to Carl's Monday meeting notes, 2026-07-06._

**Outcome: PARTIAL.** Goal A half-landed (F2 *data-plane* reached the prod server 07-08 + a written plan-of-record for the serving migration; the serving migration itself did not ship; hub finalization stayed blocked-on-ASPSI). From Wed 07-15 the **pretest went live** and Goal B's pretest reactivity legitimately became the whole lane — GPS end-of-flow (#157), the paradata `.pff` switch (#840), the GPS warm-radio patch, and the F3 Q162 Field-Control fix all shipped as live field support, ending the sprint at **fleet F1 v1.1.4 · F3 v1.1.5 · F4 v1.4.4 (2026-07-19)**.

## Carry-in from Sprint 012

| ID | Item | State | S013 disposition |
|---|---|---|---|
| **E10-ETL-EXTRACT** | `transform.py` — per-instrument questionnaire-variable extraction from the CSWeb breakout DBs per codebook (checkbox split-to-dummies; F3 pay-roster + F2 extracts; lowercased-DCF instrument vars + harmonized shared dims) | scoped; codebook v0.4 + ETL skeleton + Stata-12 do-files proven | **DEFERRED (Carl re-prioritized 2026-07-06)** — not this sprint; revisit after F2/hub. NOTE: **variable/data inconsistencies surfaced during the 197-table mapping** — will feed this extraction. Kept in Goal B. |
| **Tabulation decision memo** | 47-partial / 3-gap (tables 1.6, 2.51, 4.2) memo → ASPSI | mapping complete in `deliverables/tabulation-plan/` | Deferred with E10-ETL-EXTRACT — became Ms. Myra's 3pm tabulation meeting (2026-07-06), not a written memo. |
| **Designer compile gate** | fresh-Designer compile of the R2-numbered builds (F1/F3/F4 + hub) | F1/F3/F4 effectively CLEARED via the `.csds` route | **F1/F3/F4 re-cleared during the sprint** (07-19 warm-radio release ran full Designer compile 4/4 + CSEntry strict gate 3/3). **HUB still pending** — carried. |
| **Supervisor App finalization + UAT R6** | hub built + device-verified; needs finalization polish + hub Designer compile + deploy; R6 blocked on ASPSI account import + real names + `supervisor-qa` | roster built + deployed + device-verified (Carl-side done) | **Did not advance** — still blocked-on-ASPSI at close; a LoginApp update sits pending server-side (2026-07-15). Carried → S014. |
| **Support deliverables (rest)** | governance (privacy/backup/retention) · training finalize (decks + Survey Manual screenshots, Kidd review) | did not advance in S012 | Did not advance in S013 either (pretest took the lane). Carried opportunistic. |
| **Pretest reactivity** | freeze gate ~Jul 5; assignments + printed sheets ready | awaiting ASPSI's confirmed date | **FIRED 2026-07-15** — pretest live (Brgy. Mayondon, Los Baños, Day 1); field support took the lane for the rest of the sprint window. See Daily Notes. |
| **E4-F2-ELESTIO** | F2 Survey + Admin off Cloudflare/Google → our own prod server (initial-setup migration) | prod server now available — un-parked | **PARTIAL** — data-plane on prod 07-08 (read-mirror + unified dashboard + 12-digit QN full-stack); serving migration has a written plan-of-record (`deliverables/F2/F2-Prod-Migration-Plan.md`). Execution carried → S014. |
| **Translations Batch 2** | Tagalog-final / Ilocano / Hiligaynon-F3F4 — labels AND (new, via R2) runtime messages | blocked on ASPSI delivery | Unchanged — drop-in when delivered. |
| **F4 Option C food-roster pilot** | block-as-roster Section N rebuild running in a parallel session; on v1.2.2 | live feature stream | **FOLDED INTO MAINLINE** — the 2026-07-19 GPS merge regenerated F4 from merged sources, so Section-N Option C + GPS ship together in the deployed **v1.4.4**. Done as a tracked item. |
| **Refactor register (rest)** | R1b `library/` (Aug, with G1/G2) · F1 hand-fmf → generator fold (post-pretest) · K1 encrypted roster (Sep) | queued by design | Unchanged. |

## Committed Items — LOCKED 2026-07-06 (Mode A) · RE-ALIGNED 2026-07-06 to the Monday meeting notes · outcomes marked at the 2026-07-20 close

> Re-aligned to Carl's ASPSI-team-meeting outline: this week is **F2 to our prod server + Supervisor App finalization**, not the transform.py ETL headline the sprint locked at kickoff. ETL extraction + tabulation memo are deferred (Goal B), kept visible because the variable/data inconsistencies Carl flagged during the tabulation mapping feed them.

### Goal A — F2 onto our prod server + Supervisor App finalization

- [~] **E4-F2-ELESTIO — F2 → prod-server migration** — migrate the F2 configuration/setup off Cloudflare/Google onto our own production server (the free tier won't hold the live user load once fielded); the prod server also backs the F1/F3/F4 CSWeb sync. **CLOSE: PARTIAL → carried to S014** (data-plane done; serving migration plan-of-record written, not executed — the pretest took the lane from 07-15). `status::partial → carried` `priority::high` `estimate::2d`
  - **Wed 07-08 progress — data-plane ON the prod server:** `csweb_f2` MySQL read-mirror LIVE (41 submissions, 2-min poller cron, adversarially reviewed ×2); **unified dashboard now shows all 4 instruments** incl. F2. **12-digit QN alignment code-complete** through AS/Worker/app/spec×2/mirror (tests 202+238+518 green, `tsc -b --force` clean) — deploys Carl-gated (clasp push + `runAllMigrations()` + wrangler + staging→prod). Remaining in lane: app/admin **serving** migration off Cloudflare/Google — **PLAN OF RECORD written Wed eve: `deliverables/F2/F2-Prod-Migration-Plan.md`** (end-state call MADE: consolidate on this box — it's Elestio-managed; `uhc-hcw.asiansocial.org` vhost + f2-api container + `csweb_f2` as THE store; 8 gated phases, respondent path pretest-ready in ~3 focused days, admin port may trail). Also still queued: encoder-path qn, `f2_facility_master` seed when the frame lands.
- [~] **Supervisor App finalization (+ UAT R6)** — finalize the hub (login → menu → Bluetooth → CSWeb relay → reports → map), run the hub Designer compile, deploy, and open **UAT Round 6** the day ASPSI imports the accounts + sends real names. **CLOSE: did not advance — still blocked-on-ASPSI; LoginApp update pending server-side (07-15) → carried to S014.** `status::blocked-on-ASPSI → carried` `priority::high` `estimate::1.5d`

### Goal B — reactive / opportunistic (must not displace Goal A)

- [x] **Pretest reactivity** — assignments + printed sheets ready; freeze stands (data-integrity exceptions only). Field support jumps the queue the day ASPSI confirms the pretest date. **CLOSE: FIRED and became the real lane** — pretest live from 07-15; the item did exactly what it was designed to do (see Daily Notes 07-15 → 07-19: #157 GPS reposition, #840 paradata, warm-radio GPS, F3 v1.1.5 Field-Control fix). It displaced Goal A in week 2 — by design, but the board should have been re-locked to say so (retro Q4). `status::done (standing — continues in S014)` `priority::high`
  - **Sun 07-19 — GPS warm-radio patch SHIPPED (pretest field support, full loop in one day):** testers reported GPS capture "so slow" (weak signal inside hospital buildings). Root cause: `ReadGPSReading(120, 20)` cold-opened the radio AT the capture field (30–120 s satellite acquisition, then `gps(close)` discarded the fix so retries cold-started too). Fix: warm-radio pattern in `shared/Capture-Helpers.apc` — `WarmUpGPS()` opens the radio at the case key (fix converges while typing), captures are fresh `gps(read, 15, 20)` (~1–2 s warm; never `readlast` — no cross-case stale fixes), `ReleaseGPS()` after each instrument's last capture. Gates: preflight ALL CLEAN · Designer compile 4/4 · CSEntry strict gate 3/3 · SV tests 3/3 · **itel bench ≤2 s capture with a 5 m fix indoors**. Merged to main (`54484ed`, pushed) with F3/F4 regenerated from merged sources (Section-N + GPS both in). **Deployed to CSWeb as F1 v1.1.4 / F3 v1.1.4 / F4 v1.4.4 (2026-07-19)**; itel updated in place; patch notes posted to #f1/#f3/#f4-uat; enumerator guide + CAPI manual §8.1 updated + redeployed live. Freeze-compatible: logic-only, no data-shape change (same GPS items, accuracy still recorded). Fleet step: testers tap UPDATE per the notes.
  - **Sun 07-19 (PM) — the REAL "Field Control missing" bug found in live data: F3 Q162 skip overshoot → v1.1.5.** Testers blamed the GPS reposition for a "missing survey team (Field Control) section"; a full regression sweep found **no structural defect** (forms/ordinals/capture-types byte-consistent; breakoff + FC-tail desk-tests all PASS; saved-case persistence verified across three reads — no data leak), and the tester's own video showed a *complete* case whose end-of-flow GPS forms were auto-skipped (GPS already captured at case start on a pre-reposition build — data present, not lost). The live-data sweep then found the real pattern: **6 of 8 F3 pretest cases had the entire closing survey-team block blank** while Result-of-Visit was set. Root cause (latent since ~June, nothing to do with GPS): `PROC Q162_REFERRED` on the majority Q162=No path did `skip to ENUM_RESULT_FINAL_VISIT` — and CSPro's `skip to` jumps EVERYTHING in between, i.e. the whole closing Field Control block. Fix: retarget to the closing block's first field (`SURVEY_TEAM_LEADER_S_NAME`). Gates green → **F3 v1.1.5 stamped, deployed, itel-verified (2026-07-19)**; #f3-uat patch note incl. per-case recovery for the 6 affected cases (Modify → fill Closing block → re-sync). **Open design item flagged, NOT shipped (Carl's call):** postponed/refused/replaced outcomes `endlevel` before the end-of-flow GPS forms → those cases save with no facility GPS; proposed ~1–2 s warm read in the endlevel branch.
  - **Sun 07-19 (night) — UAT triage close-out.** Triage loop run: zero new tester activity in all 4 channels; GitHub trued up — #840 (paradata) + #843 (F4 download loss, server-side fix) closed as completed with full records; retrospective issues **#844** (GPS-slow) + **#845** (F3 Q162 FC-skip) filed + closed so GitHub stays the single source of truth for the weekend's Slack/video-reported fixes; tracker comment on #839; #842 = ASPSI-quota tracker. **Field-readiness gate: CLEAR-PENDING (exit 10)** — 0 open actionable, quiet window restarted by the weekend findings (ages out ~07-21).
- [ ] **Round 6 tester support** — support the Supervisor-hub testers once ASPSI sets up the accounts (the reactive tail of Supervisor App finalization). **CLOSE: never unblocked — carried.** `status::blocked-on-ASPSI → carried` `priority::medium`
- [x] **F4 Option C pilot** — continues in the parallel session (v1.2.x); tracked for visibility. **CLOSE: DONE — folded into mainline via the 07-19 merge; Section-N Option C ships in the deployed F4 v1.4.4.** `status::done` `priority::medium`
- [ ] **ETL extraction — DEFERRED; tabulation → Ms. Myra's 3pm meeting** — `transform.py` variable extraction stays deferred (revisit after F2/hub). The 47-clarifications/3-gaps + the **variable/data inconsistencies** from the 197-table mapping went to **Ms. Myra's 3pm tabulation meeting (2026-07-06)**. **CLOSE: deferred as planned — stays on the backlog, not an S014 commitment.** `status::deferred` `priority::medium`

## Definition of Done — Sprint 013 (trued at the 2026-07-20 close)

- [ ] **F2 Survey + Admin initial setup running on our prod server** (off Cloudflare/Google), verified reachable. **✗ PARTIAL** — data-plane yes (mirror + dashboard + QN), serving no (plan-of-record only).
- [ ] **Supervisor App finalized + deployed** — hub Designer compile clean; UAT Round 6 opened, or blocked-on-ASPSI (accounts/names) explicitly documented. **✗ / ✓** — not finalized, but the block IS explicitly documented (this line + backlog).
- [x] Any pretest-date arrival handled reactively without derailing Goal A. **✓ on the reactive half, ✗ on the "without derailing" half** — the pretest arrived 07-15 and was handled superbly (4 field patches, same-day loops), but it consumed Goal A's week 2 entirely. That trade was right; the board just never said so mid-sprint.
- [ ] Board stays honest — the sprint reflects the real week. **✗** — honest through 07-08, then silent until the 07-19 release bullet; the container ran to Day 15 of 5 un-closed, and the 07-19 board update itself missed that evening's F3 v1.1.5 arc (trued 07-20).
- [ ] **Sprint 013 retrospective filled ON TIME Fri 2026-07-10**; archived; `sprint-current.md` reset for Sprint 014. **✗ — filled 2026-07-20, 10 days late** (this close).

## Daily Notes

_Auto-standup writes here daily via the `CAPI Scrum Daily Standup MD` scheduled task (08:30 MNL) + the SessionStart hook as intraday top-up._

**Mon 2026-07-06 — Sprint 013 LOCKED (Mode A).** Goal A is deliberately singular — **E10-ETL-EXTRACT (`transform.py`)** + its **tabulation decision memo** — per the S012 retro Q4 (protect the committed build item from the reactive stream). Everything else is Goal B: pretest reactivity, F4 Option C (parallel session), hub compile + R6, governance/training. **Freeze clarified (Carl):** the pretest freeze STANDS; the weekend F1/F3 → v1.0.3 + F4 → v1.2.2 deploys (#830 checkbox ascending-order = partial-save data-loss guard; #832/#833 F4 amount-entry gate) were **data-integrity exceptions**, not a lift. **Board drift trued at lock:** the S012-close board still showed F1/F3 v1.0.2 + F4 v1.0.3 + "nothing deploys ~Jul 5"; corrected to real git state (F1 v1.0.3 · F3 v1.0.3 · F4 v1.2.2 · Hub v1.0.1) across `sprint-current.md` + `product-backlog.md`. **This drift is the S012-retro-Q2 lesson biting on schedule:** the date-drift canary stayed silent (log/board dates aligned at Friday's close) while the *version/freeze content* rotted over the weekend via the parallel/loop deploys — content rot the canary can't see. **Designer-compile gate:** F1/F3/F4 effectively cleared (weekend `.csds` deploys compile the `.pen` fresh); hub still pending.

**Mon 2026-07-06 (later) — S013 RE-ALIGNED to Carl's Monday meeting notes.** Carl updated the ASPSI-team-meeting outline; his real week = **F2 initial-setup migration to our prod server + Supervisor App finalization** (+ stay pretest-ready / support Round 6), NOT the `transform.py` ETL headline this sprint locked at kickoff hours earlier. Re-locked **Goal A → F2-prod-migration + Supervisor-App-finalization**; **ETL variable extraction + tabulation memo deferred** (kept visible in Goal B). His edit also flagged **variable/data inconsistencies seen during the 197-table tabulation mapping** — noted to feed the deferred ETL work. `E4-F2-ELESTIO` un-parked (prod server now available). Linear Sprint-013 mirror patched to match (parent goal + Goal-A sub-issues added, ETL/memo sub-issues → Canceled/deferred). Same-day re-plan, cleanly recorded — the board tracking reality rather than the morning's lock.

**Mon 2026-07-06 (later²) — re-aligned again to the updated notes + a 3pm tabulation meeting.** F2 Goal-A item refined to **F2 configuration-setup migration to the prod server** (which also backs the F1/F3/F4 CSWeb sync). **Ms. Myra requested a 3pm tabulation meeting today** — Carl shares the tabulation findings there (the 47-clarifications/3-gaps + the variable/data inconsistencies from the 197-table mapping), so the deferred "tabulation memo" is now that meeting, not a written deliverable I produce. New open constraint on the board: **"questionnaire change — when?"** — the timing of the DOH questionnaire revision governs when the freeze lifts / whether the instruments change before the pretest (ASPSI/DOH call, [[project_aspsi_doh_june_comments_parked]]). Pre-test assignments clarified as pushed to the Enumerator App from the Supervisor App **offline over Bluetooth** (not printed sheets). Linear F2 sub-issue (ANA-222) + parent updated to match.

**Wed 2026-07-08 (intraday) — E4-F2-ELESTIO: F2's data-plane is on our prod server; QN alignment built full-stack.** Three moves in the lane today: (1) **F2 read-mirror DEPLOYED** — `csweb_f2` (MySQL, same `lamp-mysql8` as the breakouts) mirrors the F2 Sheet via a 2-min HMAC-RPC poller; backfill 41/41 after widening `hcw_id` (real data: 301-char value) — the Sheet stays write-authority, so zero risk to the live survey. (2) **Unified monitoring dashboard live with all 4 instruments** (F2 = Submitted/Refusal + capture-mode + freshness; adds 0 false Partial/No-GPS). (3) **Carl decided F2 adopts the 12-digit QN** (facility-9 + HCW-seq-3, realizes the parked RR-PP-… design) — built through AS (ScriptLock seq assignment + `migrateAddQnColumn`), Worker (qn in JWT claims), app (enrollment→submission→batch), spec×2 (generator byte-identical), mirror (col live) — all backward-compatible; backend 202/202 · worker 238/238 · app 518/518 + `tsc -b --force` clean. **Carl's gates:** clasp push + `runAllMigrations()` + `/exec` redeploy · `wrangler deploy` · app staging→prod. Also: rotate the AS `HMAC_SECRET` post-cron (value transited chat during the walk). Goal A's remaining scope = the serving migration itself (box-vs-Elestio end-state call needed before Phase B).

**Wed 2026-07-15 — PRETEST LIVE (Day 1: Brgy. Mayondon, Los Baños) — the sprint's real pivot.** 7 enumerators ran F4 household; CSWeb sync worked across the fleet; one live incident — the `se-001` CSWeb login dropped by the bulk user import's header-row checkbox — diagnosed + fixed live via a one-row re-import. From here to close, the lane = reactive field support (exactly Goal B's design). The window Jul 15–17 also shipped, all field-driven and logged in `log.md`: **#157 GPS → end-of-flow** (07-16, field teams couldn't get signal at case start indoors), **#840 paradata `.pff` switch** (07-17 — F1/F3/F4 had collected ZERO paradata; v1.1.3/v1.4.3), the sync-dashboard IA redesign + case-list drill-down + responses data room (07-17/18, CSWeb reporting lane), and the **#843 F4 incremental-download server bug fix** (07-18). Two post-pretest items were designed + parked 07-15: E3-RELEASE-001 (`/capi-release` lane) and E8-SUPERVISOR-003 (hub rename + de-dupe). _[Note recorded at the 07-20 close — the board was not updated live during this stretch; that gap is retro Q2/Q4 material.]_

**Sun 2026-07-19 — the GPS weekend (full arc in the Goal B bullets above):** warm-radio patch shipped fleet-wide (F1 v1.1.4 / F3 v1.1.4 / F4 v1.4.4) → tester "Field Control missing" report triaged (regression sweep clean; video showed an auto-skip, not a missing section) → live-data sweep found the REAL bug (F3 Q162 skip overshoot, 6/8 cases) → **F3 v1.1.5** shipped same day with per-case recovery → UAT triage close-out (#840/#843 closed; #844/#845 retro-filed; gate CLEAR-PENDING). Day-by-day detail: `log.md` 2026-07-19 entries (×4).

**Mon 2026-07-20 — Sprint 013 CLOSED, 10 days late (Day 15 of 5).** The 08:30 auto-standup had been flagging "Sprint window exceeded" since 07-13 with nobody acting on it — the drift canary watches log-vs-board *content* dates, not the sprint *end* date, and an unclosed sprint keeps passing that check. Closed Mode D from the record; `sprint-current.md` reset for **Sprint 014** (pretest field support + F2 serving migration + hub/R6); Product Backlog trued (F3 v1.1.5 arc + version stamps + epic rows). Loose thread carried visibly: the **GPS-on-endlevel design decision** (refused/postponed/replaced cases save with no facility GPS).

## Retrospective — Sprint 013

> 5-minute time-box. Four questions, fixed order. Written, not thought-through-only. _Filled 2026-07-20 at the late close (Mode D — drafted from the record; the honest cost of the lateness is question 2)._

### 1. Did the sprint goal land? (yes / partial / no — one line why)

**Partial.** F2's data-plane reached the prod server (07-08) but the serving migration stopped at a plan-of-record, and the hub stayed blocked-on-ASPSI — because the pretest went live 07-15 and Goal B's reactive lane rightfully consumed the rest (4 field patches, fleet at v1.1.4/v1.1.5/v1.4.4, every tester report same-day root-caused). Value shipped all sprint; it just wasn't the committed Goal A.

### 2. What surprised me? (process, not work — max 3 bullets)

- **The sprint container can run 10 days past its end with every alarm technically working.** The standup warned "Day 15 of 5" daily; the drift canary stayed green (log and board dates moved together on 07-19). Warnings that nobody is obligated to act on are decoration — the gap is a *ritual*, not a detector.
- **A same-day board update can still drift by evening.** The 07-19 board entry recorded the morning GPS release and missed the same evening's F3 v1.1.5 — the second "content rot inside a green canary" instance after S012's. Update-at-release is the wrong checkpoint; update-at-day-end is the right one.
- **Testers attribute bugs to the most recent visible change.** The Q162 Field-Control blanking was latent since ~June, but because the GPS reposition landed the same week, the report arrived as "GPS broke Field Control." The regression sweep + video + live-data forensics sequence (structure → footage → data) was what separated correlation from cause — worth keeping as the standard triage ladder.

### 3. Deadline exposure check — D2 / D3 / Tranche slip days this sprint

_Informational only (out of Data Programmer scope per CSA D1–D6)._ The pretest actually running (07-15 →) is schedule-positive: it restarts the D4 chain that had been ASPSI-gated since June. No new Carl-side slip; the six affected F3 cases have a posted field-side recovery path.

### 4. One thing to change in Sprint 014

**Close-or-roll on the FIRST "Sprint window exceeded" standup warning — as a Monday-morning ritual, not a discretionary alert.** Concretely: the day the standup says the window lapsed, either close the sprint (Mode D from the record, even if thin) or re-lock it with new dates — the 07-06 same-day re-alignment proved the muscle exists; S013 just never used it once the pretest hit. Corollary written into S014's DoD: when a reactive stream takes the lane mid-sprint, re-lock the board that day.
