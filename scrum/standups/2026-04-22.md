---
date: 2026-04-22
project: UHC Survey Year 2 — CAPI Development
sprint: 002
sprint_day: 3 of 5
sprint_status: active
---

# Daily Standup — 2026-04-22, Wednesday

**Project:** UHC Survey Year 2 — CAPI Development
**Sprint:** 002 — **Close F1 design cleanly, open the logic-pass gap on F3 and F4, and submit Tranche 2 as an honest status-to-date package.** Day 3 of 5. Tranche 2 (40%) due Fri 2026-04-24 — 2 days out.
**Sprint Day:** 3 of 5

---

## Headline

**Day 3 focus: Epic 3 kickoff — E3-F1-001 FMF Section A.** Pre-flight check surfaced a plan/DCF drift (form-layout plan referenced 7 case-start fields not in the DCF). Consulted Carl on resolution; he picked **(b) extend DCF** + **1b parallel across F1/F3/F4** + **2a AAPOR 2023 standard codes**. Case-control extension landed mid-morning; now proceeding to `generate_fmf.py` build.

## Yesterday (2026-04-21, Day 2)

See `standups/2026-04-21.md`. Net: F3 + F4 specs reviewed (Build-ready), F1 spec aligned, F4 schema CLOSED-BY-VERIFICATION, shared form-layout-principles + per-instrument plans shipped.

## Done (today)

- **Case-control extension — `cspro_helpers.py`.** Added `AAPOR_DISPOSITION_OPTIONS` (AAPOR 2023 10th ed., 11-code 3-digit zero-filled value set) + `_case_control_items(survey_code)` helper returning 5 items (SURVEY_CODE alpha-2, INTERVIEWER_ID numeric-4, DATE_STARTED YYYYMMDD, TIME_STARTED HHMMSS, AAPOR_DISPOSITION with AAPOR VS). Modified `build_field_control()` to require `survey_code` param and prepend the case-control block. Baked `FACILITY_NAME` (alpha-100) + `FACILITY_ADDRESS` (alpha-200) into `build_geo_id("facility")` for F1.
- **Case-control extension — generators.** F1 `generate_dcf.py` imports `_case_control_items` and prepends to its local `build_field_control()`. F3/F4 generators pass `survey_code="F3"` / `"F4"` to the shared helper. All three DCFs regenerated cleanly: **F1 12 records / 671 items (+7), F3 18 records / 840 items (+5), F4 22 records / 623 items (+5)**. Deltas match exactly.
- **Case-control extension — specs.** F1 §4.17, F3 §4.16, F4 §4.12 — preproc templates using `sysdate("YYYYMMDD")` + `systime("HHMMSS")` + `tonumber()` for timestamps, literal assignment for SURVEY_CODE, `AAPOR_DISPOSITION = 0` (000 In Progress) initial + `210` (Refusal — respondent) on consent refusal. Footer item counts updated.
- **F1 form-layout plan — Cat 1 typos fixed.** `PROVINCE` → `PROVINCE_HUC`, `CITY_MUN` → `CITY_MUNICIPALITY`, `Q2_DESIGNATION` → `Q2_FACILITY_ROLE`. Dropped fictional `Q2_DESIGNATION_OTHER_TXT` row (DCF has 11 exhaustive options, no Other branch). Form 4 row cost 8 → 7. Form 1 AAPOR note aligned with actual DCF code (`000` not `1`).
- **Scrum artifacts synced.** Epic 2 F1/F3/F4 scope rows + Designer sign-off task descriptions updated with the new block + counts. Epic 3 current-state rows updated. Product-backlog F1/F3/F4 rows updated. Sprint-current Day 3 entry added.
- **F3 DCF duplicate-item bug fixed.** Root cause: `select_all()` in `cspro_helpers.py` auto-appends `_OTHER_TXT` when the last option has "specify", but 34 section-builder calls in `deliverables/CSPro/F3/generate_dcf.py` also added the same item explicitly — creating duplicate DCF entries across 9 records. Fixed by removing all 34 redundant explicit `alpha(_OTHER_TXT)` calls. F3 DCF regenerated: **18 records / 806 items** (down from 835 pre-case-control / 840 post-case-control — net 34 items removed, no orphans). Zero duplicates confirmed by script.
- **F3 FMF skeleton verified clean.** `generate_fmf.py` re-run against fixed DCF: 19 forms, 0 orphan items. `PatientSurvey.generated.fmf` updated. Form-level skips and Designer splits unchanged.
- **F4 DCF + FMF verified clean (no fix needed).** `generate_dcf.py` + `generate_fmf.py` re-run: 22 records / 623 items, 0 duplicates, 24 forms, 0 orphan items. F4 generators were written correctly from the start — the `_OTHER_TXT` duplication pattern was F3-only.
- **Both F3 and F4 now at Designer stage.** Next step for each: open `{Instrument}.generated.fmf` in CSPro Designer, do form splits per layout plan, wire form-level skips, add PROC logic from skip-logic specs.

## Tomorrow / remaining sprint (planned)

1. **E2-F1-010** F1 Designer sign-off — open `FacilityHeadSurvey.dcf` + `.fmf` in CSPro Designer, walkthrough, bug list. Critical path for Tranche 2.
2. **E0-032a** DOH-PMSMD feedback triage — route any matrix revisions into F1/F2 build state; confirm Tranche 2 submission package with Juvy by Apr 23.
3. **E0-010** Weekly status format — draft for Juvy/Dr Claro/Dr Paunlagui.
4. **Sprint 002 retro** EOD Fri 2026-04-24; archive to `scrum/sprints/sprint-002.md`.

## Blocked

- PSGC value sets still pending ASPSI-side.
- Q31 IP_GROUP list (F3) still pending Juvy.
- Q15 IP_GROUP list (F4) still pending ASPSI.
- Q63 day vs month buckets (F1), SECONDARY_DATA structure (F1) — both to ASPSI.

## Notes

- **DCF re-review scope bump:** E2-F1-010 / E2-F3-010 / E2-F4-010 Designer sign-offs now include the case-control block. Scope noted in each task description; no new tickets opened.
- **Preproc PROC wiring** is Epic 3 work — lands with the FMF build, not the DCF extension. Templates are in the skip-logic specs ready to paste into the `.apc`.
- **Process flag:** second time this week a pre-flight verify step caught a drift before a destructive edit (F4 ghost-patch yesterday; plan/DCF drift today). Feedback memory `feedback_verify_before_patch.md` is earning its keep.
