---
date: 2026-04-14
project: ASPSI-DOH-CAPI-CSPro-Development
sprint: 001
sprint_day: 2 of 5
backfilled: 2026-04-16
---

# Daily Standup — 2026-04-14, Tuesday

**Project:** UHC Survey Year 2 — CAPI Development
**Sprint:** 001 — *Close F1 Design, Unblock F1 Build* (Apr 13 → Apr 17)
**Sprint Day:** 2 of 5

> Backfilled 2026-04-16 from `sprint-current.md` Daily Notes, `log.md`, and commit history. Standup cadence broke Apr 14–15 — content landed in Daily Notes instead of standalone files. Reconstructed to restore day-by-day record for Sprint 001 retro (Apr 17).

---

## Yesterday

- **2026-04-13 PM**: Attended 3:00 PM LSS meeting with Doc Myra and team. Filed yesterday's end-of-day note declaring "no schema changes required; DCF v2 final" and closed E2-F1-009b + promoted E3-F1-001 from stretch to committed. **That note was wrong** — see today's correction.

## Today

- **Correction to yesterday's note (high priority).** The Apr 13 LSS meeting did NOT actually discuss any of the 6 open F1 items. Agenda was process-focused (lessons learned, comms lines, D2/D3/D4 tasking). E2-F1-009b is **reopened** and flipped to `blocked` in `epic-02-questionnaire-design.md` and `sprint-current.md`. Yesterday's end-of-day note is preserved with a correction entry beneath it.
- **Audit finding on E2-F1-009**: the prior scrum narrative claimed `generate_dcf.py` existed and a 952-item DCF was generated Apr 11. Reality on disk: only Carl's manual ~105-item scaffold at `raw/CSPro-Data-Dictionary/FacilityHeadSurvey.dcf`, no Python generator. **E2-F1-009 was actually the work done today, not Apr 11.**
- **Built `deliverables/CSPro/F1/generate_dcf.py` from scratch.** Used the manual scaffold for Q1–Q8 format conventions (preserving exact item names) and `F1-Skip-Logic-and-Validations.md` for canonical Q9–Q166 names. First output: `FacilityHeadSurvey.dcf` with **15 records, 657 items**. Secondary-data records (SEC_HOSP_CENSUS, SEC_HCW_ROSTER, SEC_YK_SERVICES, SEC_LAB_PRICES) left as empty stubs pending LSS decision. The 6 LSS-blocked items encoded as `PENDING_LSS_*` constants at the top of the script — flip + rerun regenerates affected schema.
- **E2-F1-010 — Designer walkthrough round 1.** Ran the DCF through CSPro Designer and applied 5 changes back in `generate_dcf.py` (per the "generator is source of truth" rule):
  1. `QUESTIONNAIRE_NO` promoted out of FIELD_CONTROL into the level ID (replaces placeholder, length 6, zero-fill; keys every record).
  2. `ENUM_RESULT` renamed → `ENUM_RESULT_FIRST_VISIT`; parallel `ENUM_RESULT_FINAL_VISIT` added (same 4-option value set).
  3. `EDITED_RESULT` deleted — superseded by the two visit-result fields.
  4. `RESP_NAME` / `RESP_POSITION` / `RESP_EMAIL` / `RESP_MOBILE` moved out of FIELD_CONTROL into `A_FACILITY_HEAD_PROFILE`.
  5. REGION / PROVINCE_HUC / CITY_MUNICIPALITY / BARANGAY value sets flagged **PSGC-blocked** — remain plain numerics until ASPSI delivers the code lists. **New blocker.** Counts after round 1: 15 records, 656 items.
- **E2-F1-010 — verbatim labels wide pass (Sections C–H).** Established "verbatim questionnaire text in DCF labels" as the standing rule (per memory + wiki). Rewrote every paraphrased label in `build_section_c` through `build_section_h` against `inputs/F1_clean.txt`. CSPro's 255-char label limit forced two trims: Q43 (ward allocation parenthetical dropped, stem preserved) and Q88 (PhilHealth tranche mechanics compressed). Counts after wide pass: 15 records, 655 items.
- **E2-F1-010 — secondary data records removed.** Dropped the four `SEC_*` PENDING-LSS stubs from `build_dictionary()` per Carl's call to exclude secondary data from F1's dictionary entirely. Counts after drop: **11 records, 655 items** (final for today).
- **NA code convention ratified.** Decided rule: **NA = highest valid code at the item's field width** (9 for length-1, 99 for length-2, 999 for length-3) — not DHS 7/97, because F-series doesn't track Missing as a distinct outcome. Never use CSPro's reserved `notappl` in value-set entries. Applied: `Q103_REASON` and `Q110_REASON` renumbered (NA `4` → `99`). Saved as memory + new wiki concept page `F-Series Value Set Conventions`. Carries to F2/F3/F4.
- **`/raw/` hygiene**: moved `raw/Project-Deliverable-1/F1_clean.txt` → `deliverables/CSPro/F1/inputs/F1_clean.txt`. Internal text extraction, not an ASPSI source — doesn't belong in immutable raw zone.

## Blockers

- **E2-F1-009b REOPENED / BLOCKED.** The 6 open F1 items are still pending an LSS-style decision. Need Dr Myra / Dr Claro to convene a **technically-scoped** LSS session. Default assumptions are encoded as `PENDING_LSS_*` constants so the DCF still loads — but sign-off on v2-final requires the decisions.
- **NEW — PSGC value sets blocked on ASPSI.** REGION / PROVINCE_HUC / CITY_MUNICIPALITY / BARANGAY code lists not yet delivered. F1 items remain plain numerics; blocker will inherit to F2/F3/F4. Added to E0-032 exposure watchlist.
- **External**: SJREB clearance still pending (recurring via E0-020). Long-pole for Epic 6, not a Sprint 001 blocker.

---

## Sprint Board Snapshot (end of Day 2)

| ID | Item | Status |
|---|---|---|
| E0-001 | Sprint 001 planning ceremony | `done` |
| E0-020 | SJREB status check | `in-progress` (recurring) |
| E0-032 | D2/D3 deadline tracking | `in-progress` (recurring); PSGC added to watchlist |
| E0-060 | Attend Apr 13 LSS meeting | `done` |
| **E2-F1-009** | **Generator + DCF from scratch** | **`done` (Apr 14 — previously mislabeled Apr 11)** |
| **E2-F1-009b** | Reconcile DCF with LSS decisions | **`blocked` (reopened)** — defaults encoded as `PENDING_LSS_*` constants |
| E2-F1-010 | Designer walkthrough + sign-off | `in-progress` — round 1 walkthrough done (5 patches + verbatim labels + secondary drop + NA convention); round 2 pending on LSS unblock |
| E3-F1-001 | F1 form file + Section A layout | `todo` (committed; unblocked for non-`PENDING_LSS_*` portion) |
| E0-010 | ASPSI weekly status format | `todo` (stretch) |

---

## Notes

- **Process correction filed.** Yesterday's end-of-day optimism ("Sprint 001 is now poised to overshoot its goal") was based on a misread of what the LSS meeting covered. Lesson for Sprint 001 retro: standup notes must distinguish what I heard-about-the-meeting from what happened-in-the-meeting. Carl was not an attendee per the Apr 13 participant roll; the claim should not have been filed as a green-light without the actual minutes.
- **"Generator is source of truth"** rule now enforced end-to-end. All F1 DCF mutations flow through `generate_dcf.py`; no hand-edits in Designer. Designer is the validation surface, not the edit surface. Saved as memory earlier; today was the first full cycle under the rule.
- **Deadline exposure (D2):** unchanged at Apr 24 (no slippage today). D2 critical path still runs through Tranche 1 acceptance → SJREB → pretest → pre-tested tools sign-off.
- **Shan name contradiction** surfaced in ingested meeting minutes (Apr 13 docx uses "Shan Lait"; earlier wiki entry was "Sean"). Flagged — not touching the 16 backlinks without Carl's confirmation.
