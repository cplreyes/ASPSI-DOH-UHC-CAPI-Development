# fixtures

Small, committed sample inputs for testing `aug17-tools/*.py` scripts against —
kept separate from the gitignored `instruments-aug17-extract/` working area so
the scripts have something stable to run against in CI/local dev.
