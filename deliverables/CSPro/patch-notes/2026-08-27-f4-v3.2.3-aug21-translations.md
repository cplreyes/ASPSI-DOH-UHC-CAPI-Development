# F4 v3.2.3 — Aug-21 alignment + translations (wave 3)

Patch note for the F4 leg of the Aug-21 translations plan
(`docs/superpowers/plans/2026-08-25-aug21-translations.md`). Deployed 2026-08-27
(v3.2.0/3.2.1/3.2.2 were deployed 2026-08-26 and are superseded).
The Slack post is the block below; the sections after it are the build record.

`*Changed:*` is used instead of the skill template's `*Fixed:*` — this build is an
alignment + import, not a bug fix (the two genuine fixes in it, the Waray
question-number strip and the Filipino bracket strip, are called out on their own lines).

> **POSTED 2026-08-27 ~13:50 MNL to #f4-uat** (tester-facing edit of the block below: same facts, build-history bullets folded into plain 'fixed' bullets, evidence links expanded): https://aspsi-doh-uhc-survey2.slack.com/archives/C0B60KYFBKN/p1787805076974789

---

🔧 **Household Survey (F4) — patch deployed (v3.2.3)**
*Changed:* The app now matches ASPSI's **Aug-21 revised questionnaire** and carries the **Aug-21 translations in all 7 languages** (Filipino, Bikol, Bisaya, Cebuano, Waray, Hiligaynon, Ilocano). Of the 1,401 translatable labels: FIL 915 (65%), BCL 947 (67%), BIS 931 (66%), CEB 975 (69%), WAR 1001 (71%), HIL 828 (59%), ILO 954 (68%) — up from 845 (60%), 868 (61%), 856 (61%), 902 (64%), 920 (65%), 701 (50%), 836 (59%).
- Q30 roster name prompt now reads "Name (Write the complete name of HH member)". The old "LAST NAME, FIRST NAME & MIDDLE NAME, EXT" format instruction is off the Aug-21 paper and is therefore off the screen too — **name order is now a briefing point, not something the app tells the enumerator.**
- Q35 "With disability?" / Q36 "Would the patient like to specify the type of disability?".
- Q40 now reads "Highest level of education **completed**" — this reverses the earlier tester ruling #608 ("attended/reached"); the DOH-submitted paper wins.
- Q67 now asks "How much time does it take **for you** to reach the nearest pharmacy from your home?"; the paper's pharmacy definition (FDA LTO) prints once, as the blue note under the question, not inside the stem.
- Q117/Q118 show the paper's "[Answer only “yes” in Q112]" and Q131/Q135 "[Ask only if they went to a DOH-retained hospital]" as blue notes; routing is unchanged (the app already skips them automatically). Those four gate notes print in English in every language.
- The consent screens and the section intros now read in the selected language (Aug-21 cleared translations); paragraphs without a cleared translation stay English.
- **v3.2.1 supersedes v3.2.0, which was live for about an hour this afternoon:** Waray screens no longer repeat the paper's question number in front of the translated text. Q69 (GAMOT) is the one to check: it reads "Nakabati ka na ba han Guaranteed and Accessible Medications for Outpatient Treatment (GAMOT) Package…", not "69. Nakabati ka na ba han Guaranteed…". If your app list still says v3.2.0, re-add the app. (Q27/Q28/Q29 are NOT the ones to check — those three read in English, see *Known:* below.)
- **v3.2.2 supersedes v3.2.1:** Filipino screens no longer carry the Aug-21 Tagalog paper's square brackets around the translated text. Q3 is the one to check: the options read "Lalaki" and "Babae", not "[Lalaki]" and "[Babae]", and the question reads "Ano po ang inyong kasarian nang ipinanganak?" without the brackets. 459 Filipino values were affected — about half of everything read aloud in Filipino. If your app list still says v3.2.1, re-add the app.
- **v3.2.3 supersedes v3.2.2:** thirteen option rows were showing a NEIGHBOURING option's translation, so two choices on the same screen read exactly the same. Waray Q13 is the one to check: "Working for private household" and "Working for government" now read differently — the first shows in **English**, because the Waray paper printed the government text against both. Waray Q52/Q55/Q58/Q70/Q127/Q133/Q137 "LGU/Barangay" now reads in English too, instead of repeating "Balaud" from the "Legislation" row above it; Waray Q128/Q134 code 5 and Filipino/Ilocano Q45.2 the same way. **A row that reads in English here is the fix, not a miss** — the paper carries no separate translation for it, and an English label a tester can tell apart beats a wrong one they cannot. Full list in the *row-inheritance repair* section below. If your app list still says v3.2.2, re-add the app.
- No question numbers, codes or data shape changed.
*Known:*
- **7 Waray screens read in English**: Q27 refrigerator, Q28 television, Q29 washing machine (question + options) and one "result of visit" option. The Waray paper numbers those one behind the CAPI, so its text may answer a different question; they are held until a translator rules.
- **13 option rows read in English from v3.2.3** (10 Waray, 2 Filipino, 1 Ilocano) — listed in the *row-inheritance repair* section. That is the fix, not a miss: each of them was showing a neighbouring option's translation, and the paper carries no separate text for them. They are on the translators' worklist.
*To get it:* In CSEntry, **remove Household Survey, then Add Application → from CSWeb**. You're on the new build when the app list shows **Household Survey (F4) - v3.2.3 (2026-08-27) [DEV]**. (⋮ → Update Installed Applications is unreliable.)
Cases already in progress are unaffected.
*Still English on some screens?* That item had no cleared Aug-21 translation for that language — it is not a build defect; the list of those items is with ASPSI's translators.
Evidence: https://raw.githubusercontent.com/cplreyes/ASPSI-DOH-UHC-CAPI-Development/507406e5a64654110f5760cc0796216e10ceb2a3/docs/uat-fix-evidence/2026-08-26-aug21-translations/F4/00-app-list-f4-3.2.3.png · .../byte-verify-3.2.3.txt · .../dcf-removal-proof-3.2.3.txt

---

## Coverage (labels translated, of 1,401)

Both rows counted by the rule `apply_translations()` uses — a name-scoped map key that
names a labelled node — against the **same** Aug-21 English key set, so the delta is the
translation maps alone and not the English edits
(`.superpowers/sdd/2026-08-25-aug21-translations/task-30/_coverage_delta.py`; the pre-wave
side is read from `git show HEAD:`, since all wave work is uncommitted).

| | FIL | BCL | BIS | CEB | WAR | HIL | ILO |
|---|---|---|---|---|---|---|---|
| pre-wave (3.1.4) | 845 (60%) | 868 (61%) | 856 (61%) | 902 (64%) | 920 (65%) | 701 (50%) | 836 (59%) |
| v3.2.2 (superseded) | 915 (65%) | 947 (67%) | 931 (66%) | 975 (69%) | 1001 (71%) | 828 (59%) | 954 (68%) |
| **shipped (3.2.3)** | **915 (65%)** | **947 (67%)** | **931 (66%)** | **976 (69%)** | **990 (70%)** | **825 (58%)** | **955 (68%)** |
| delta vs pre-wave | +70 | +79 | +75 | +74 | +70 | **+124** | **+119** |

Every language gained. Coverage counts key presence, so a cell could only have fallen if an
override had deleted a key. WAR is +81 rather than v3.2.0's +88 because v3.2.1 removes the
seven Waray rows whose paper number contradicts the CAPI's (below). Percentages are
truncated, not rounded — the same convention `generate_dcf.py` prints.

The shipped row was re-measured against the **v3.2.3** maps. WAR **falls 11** and HIL **3**
against v3.2.2, and that is the point of this build rather than a regression: those keys held a
neighbouring option's text or a truncated fragment, they now render the English label, and coverage
counts key presence. CEB and ILO each gain one key from the current extractor. Filipino is
unchanged in count — its two removals are offset by two new keys.

The v3.2.2 row is kept above it because a tester who installed that build this week is reading a
different pack.

The generator's own line counts labelled *nodes* (1,403) rather than keys and therefore
prints figures two higher; the delta is unaffected because both sides of the table above are
counted the same way.

Note layer (enumerator instructions and section intros), `notes_lookup.coverage()`:
FIL 50 · BCL 45 · BIS 45 · CEB 26 · WAR 46 · HIL 43 · ILO 37 — equal to the non-regression
floor pinned in `F4/test_aug21_f4.py` (`NOTES_FLOOR`), i.e. the build does not lose a note.

## Held this build

`aug21-overrides.json["F4"]` holds **99 translation-map keys / 244 locale-scoped rows** — 82
`keep: null` ("never write"), 4 `keep: "<text>"` (an accepted flagged span, see the v3.2.3
section) and 13 `remove: true` (delete the row, render English) — plus 58 `note:`/`icf:` entries
from the notes and consent layers. Every hold is scoped to the locale whose paper carries the defect, so a one-paper
problem never suppresses the same key's correct writes in the other six maps. (The count
grew 217 → 221 rows when the note-layer detector was added in Task 28's fix round, then
→ 224 when three entries were widened to Waray for v3.2.1, and → 244 for v3.2.3: 13 `remove`
rows, 4 accepted spans and 3 existing holds widened to the locale the current extractor
poisons.)

None of these is a code defect. Each is on the extractor or translator worklist.

1. **Extractor-class holds — the paper's furniture glued onto the translation.** Five
   families, all held rather than imported: the GAMOT applicability note glued to Q70–Q73
   (and the FIL Q38 PWD-card note, the same class), the Q17 English definition block, the
   Q18 income directive ("Tick the income category …"), the Q66/Q74 local "select all that
   apply" repeats, and the HIL Q28/Q29 quoted-question `”` glyphs. `NOTE_PATTERNS` in
   `anchor_extract.py` is the natural home for the first three; the fix is a wave, not a
   patch, because it re-opens the extract for F1 (LIVE) as well.
2. **7 Waray paper-number-mismatch rows.** `item:`/`vs:Q27_REFRIGERATOR` (paper prints
   `26.`), `item:`/`vs:Q28_TELEVISION` (`27.`), `item:`/`vs:Q29_WASHING_MACHINE` (`28.`) and
   `val:ENUM_RESULT_FINAL_VISIT_PICK_VS1:4` (the paper collapsed the whole four-option grid
   into one label). The Waray paper's numbering runs one behind the CAPI's on these rows, so
   the text may answer a different question. Waray falls back to **English on screen** until
   a translator rules; the text itself reads plausible, so the ruling may well be "accept
   all seven", which is a one-line locale-scoped `keep:` override, never a hand edit.
3. **`val:Q88_DIFF_PAYING_VS1:04` still trails off into the next section's intro**
   (`intro:89`) in all seven maps (`… We will now ask questions about your access to a`).
   **Pre-existing** — it is in
   the pre-wave baseline, this wave did not introduce it, and the Aug-21 extract offers no
   clean replacement, so repairing it would mean a hand edit of a map outside the apply
   path, which the wave rules forbid.
4. **The Section-N intro (`intro:144`) renders English in all seven locales, deliberately.**
   Task 29's `_canon()` dash fold made it reachable for the first time; what it reached are
   captures that are 4–9% of an 865-character read-aloud script (one opening clause, none of
   the household-consumption instructions). All seven are held at `keep: ""` until
   `find_translation()` can span a multi-paragraph intro. Reading a fragment aloud is worse
   than reading the English.
5. **The four printed gates (Q117 / Q118 / Q131 / Q135) print English in all 8 languages.**
   The extract recovers nothing for those bracketed directives, so the standing
   `note:const:_GATE_*` holds keep English on screen. Q118 carries a second instruction
   paragraph that *does* translate independently (FIL "BASAHIN ANG PAGPIPILIAN…", WAR
   "BASAHA HA MAKUSOG…", ILO "(BASAEN TI OPTIONS…)").
6. **No longer held — the FIL square-bracket gloss, fixed in v3.2.2 (Task 33b).** Kept in
   this list because v3.2.0 and v3.2.1 both shipped it, so a tester who installed either
   this afternoon saw it on screen. v3.2.1 wrapped **459 of 948** Filipino values wholly in
   `[ … ]` — all 459 written by this wave, the pre-wave map had 0 and the other six locales
   0 each. Cause: the Aug-21 **Tagalog** F4 paper is bilingual — it prints the English line
   with the Filipino gloss in brackets after it (`☐ Male [Lalaki]`) — and the extractor kept
   the delimiter as sentence text, the same class of defect as the Waray question-number
   prefix v3.2.1 fixed. `anchor_extract.py` now drops one whole-value bracket pair; measured
   on the shipped maps, **no F4 value in any of the seven locales is wholly bracketed**.
   Residual: 11 values still carry a bracket somewhere inside them (8 fil, 2 hil, 1 war)
   against 19 pre-wave — span-boundary residue, not the gloss convention, on the worklist
   and not hand-editable. The F3 Wave-4 import must run on this extractor: the Aug-21
   Tagalog F3 paper has 503 bracketed lines.

## v3.2.3 — the row-inheritance repair (Task 49)

The whole-branch review of this wave found a defect class the per-task reviews structurally could
not see. An option row that silently carries a **neighbouring** row's translation is well-formed and
in the right language, so no extractor flag fires — but two codes of one value set then read
**identically on the tablet**, the enumerator cannot tell the two choices apart, and the analyst
cannot tell the two answers apart afterwards.

Two mechanisms, both read off the Aug-21 papers (`task-48-report.md` §2): the paper lays an option
grid out in two columns, so one row's span comes back empty and the whole block falls to its
neighbour; and the paper itself prints one translation against two or three English rows.

Task 48 fixed the **extractor** (it now holds such a row instead of writing it) and added a
permanent `duplicate-label` gate to `apply_aug21.py` over the map an apply WOULD leave behind — the
only place both sides of a collision are visible. That stops the class being re-introduced; it
cannot repair what is already on disk, and for the thirteen rows below the paper carries **no
distinct translation at all** (their pre-wave values are the same neighbour's text with a stray
legend digit, or a truncated fragment). The honest outcome is to leave the row untranslated: a new
`remove: true` override in `aug21-overrides.json` deletes the map entry and the English label
renders. **An English option label a tester can tell apart beats a wrong one they cannot.**

### Removed — these thirteen rows now read in ENGLISH

| row | now reads (English) | read on v3.2.2 |
|---|---|---|
| `val:Q45_2_WHY_NOT_REG_VS1:02` (fil) | Don't see value in registering | code 01's gloss `Mahirap magparehistro` |
| `val:Q45_2_WHY_NOT_REG_VS1:03` (fil) | Don't know how to register | code 01's gloss `Mahirap magparehistro` |
| `val:Q45_2_WHY_NOT_REG_VS1:08` (ilo) | No valid ID to register | code 07's `Awan ti oras nga agparehistro` |
| `val:Q128_NBB_UNDERSTAND_VS1:05` (war) | Medicine and service are already included | code 03's `Mababayaran han PhilHealth an gastos han pagtambal` |
| `val:Q134_ZBB_UNDERSTAND_VS1:05` (war) | Medicine and service are already included | code 03's `Mababayaran han PhilHealth an gastos han pagtambal` |
| `val:Q13_INCOME_SOURCE_VS1:02` (war) | Working for private household | code 03's `Pagtrabaho ha gobyerno` (= *working for government*) |
| `val:Q52_UHC_SOURCE_VS1:06` (war) | LGU/Barangay | code 02's `Balaud` (= *legislation*) |
| `val:Q55_YAKAP_SOURCE_VS1:06` (war) | LGU/Barangay | code 02's `Balaud` |
| `val:Q58_BUCAS_SOURCE_VS1:06` (war) | LGU/Barangay | code 02's `Balaud` |
| `val:Q70_GAMOT_SOURCE_VS1:06` (war) | LGU/Barangay | code 02's `Balaud` |
| `val:Q127_NBB_SOURCE_VS1:06` (war) | LGU/Barangay | code 02's `Balaud` |
| `val:Q133_ZBB_SOURCE_VS1:06` (war) | LGU/Barangay | code 02's `Balaud` |
| `val:Q137_MAIFIP_SOURCE_VS1:06` (war) | LGU/Barangay | code 02's `Balaud` |

### Kept — the code whose English the text actually translates

Each is an **accepted flagged span**, expressed as a `keep` override, never a hand edit of a map:

| locale | row | keeps |
|---|---|---|
| fil | `val:Q45_2_WHY_NOT_REG_VS1:01` | `Mahirap magparehistro` — the translation of `Difficult to register` |
| ilo | `val:Q45_2_WHY_NOT_REG_VS1:07` | `Awan ti oras nga agparehistro` — `No time to register` |
| war | `val:Q128_NBB_UNDERSTAND_VS1:03` | `Mababayaran han PhilHealth an gastos han pagtambal` — `PhilHealth will cover cost of treatment` |
| war | `val:Q134_ZBB_UNDERSTAND_VS1:03` | the same string, same English |

war `val:Q13_INCOME_SOURCE_VS1:03` `Pagtrabaho ha gobyerno` and the seven war
`val:Q*_*_SOURCE_VS1:02` `Balaud` rows keep their pre-wave values and need no override:
nothing overwrites them once the inheritor is gone.

### What else moved, and why

Re-applying the whole wave from the pre-wave baseline with the fixed extractor also changes rows the
extractor now **holds** and rows a later extractor round now reads better. Every changed row was
classified; there were no unexplained ones.

* **23 rows revert to their pre-wave value** because the fixed extractor holds the Aug-21 span:
  10 WAR `Health center/facility` rows lose the inherited `Iba pa nga parag-ataman han panlawas …`
  prefix and read `Health center/Pasilidad` again, and 13 `truncated-tail` rows drop a fragment
  (3 HIL `Wala kami sang amo sina`, 1 WAR `Dire ako nahinunumdom in`, and 7 ILO / 2 BCL-BIS rows).
* **15 item/value-set labels** are the current extractor's own reading — v3.2.2 was built before the
  Task-40 and Task-48 rounds. Improvements: the BCL Q17 English definition block and the ILO Q74
  `Pilien amin nga agaplikar.` instruction stop riding into the question text, `Kantidad ha Pisos` is
  swept off WAR `val:Q142_OTHER_YN_VS1:1`, two HIL quoted-question `”` glyphs go, and six keys the
  live maps did not hold at all (FIL Q66/Q74, CEB Q61, ILO Q17 pair) arrive translated.
* **3 rows are newly held**: WAR `item:Q73_GAMOT_MEDS_LIST` (the span opens on the paper's orphan
  `] 73 .`) and the ILO `Q29_WASHING_MACHINE` item + value-set pair (the span glues the next
  question's enumerator instruction on). All three are keys the live map does not hold, so the hold
  simply keeps the English that F4 3.2.2 already shows.

### Gates

`apply_aug21.py --only F4 --fail-on-pre` reports **0 violations on a row this apply writes and 0
un-ruled pre-existing sets**, exit 0 — the STRICT path an instrument publishes on, with
`duplicate_label_accepted.json` still **empty** (nothing was ruled benign to get there).
`_defect_sweep.py --inst F4 --fail-on-pre` exits 0. The post-apply replay writes 0, replaces 0 and
removes 0 in all seven locales.

## Paper-side defects (ASPSI worklist — not fixable in CAPI)

* **Ten Waray/Hiligaynon values still carry a question number, every one pre-existing.**
  WAR `item:`/`vs:Q52_UHC_SOURCE` (prints `53.`), `vs:Q65_CONDITIONS_VS1`,
  `vs:Q93_WHY_NOT_VS1`, `item:`/`vs:Q143_HOW_PAID`, `val:ENUM_RESULT_FIRST_VISIT_VS1:4` and
  `val:ENUM_RESULT_FINAL_VISIT_VS1:4`; HIL `val:Q6_CIVIL_STATUS_VS1:8` and
  `val:Q39_CIVIL_STATUS_VS1:8`, which carry the English `40. Highest level of education`.
  All predate the Aug-21 wave and the Aug-21 papers offer no clean replacement. WAR carried
  41 such rows before the wave and 195 after v3.2.0; v3.2.1 leaves 8.
* **The Waray paper's question numbering runs one behind the CAPI's** on Q27/Q28/Q29 and on
  the result-of-visit grid — the source of the seven holds above.
* **The Aug-21 Tagalog F4 paper is bilingual** (English line + bracketed Filipino gloss)
  where the other six are monolingual. That layout, not a CAPI defect, is what produced the
  FIL brackets v3.2.0/v3.2.1 shipped. The paper is unchanged; v3.2.2's extractor drops the
  pair on import, and F3's Wave-4 import meets the same layout.
* **Q118's English stem still diverges from the paper** by more than the leading gate
  bracket (the #658 CAPI stem). Left as-is on purpose: the dcf label is the anchor
  `anchor_extract.py` searches the dialect PDFs with, so re-wording it to the paper would
  strand Q118 in English in all seven dialects.
* **Build-vs-paper checker residual: 35 rows, real misses = 0.** 29 are parser artefacts
  (the Aug-21 paper prints those questions in a table whose number sits in its own cell, so
  the PDF dump emits a bare `42.` / `186.` line and no `N. <text>` key forms) and 6 are
  intentional CAPI wording (Q2 split into month + year; the four gate-bracket rows; Q157's
  disambiguated `Sub-total`). Two artefacts the plan predicted did **not** fire: the paper
  has exactly one `67.` line, and the `HH:MM` component suffixes now match.

## Build and deploy record

| | |
|---|---|
| version | **3.2.3** (`versions.json` date `2026-08-27`, channel `dev`) — PATCH on 3.2.2, which was PATCH on 3.2.1, which was PATCH on 3.2.0, which was MINOR on 3.1.4 |
| `.pff` Description | `Household Survey (F4) - v3.2.3 (2026-08-27) [DEV]` |
| served package | `root@207.148.65.115:/opt/app/lamp/www/csweb/files/apps/HouseholdSurvey.zip` |
| served zip | 1,672,171 bytes, md5 `a56f24ab7d797dc005b3322765bd5e23`, written 2026-08-26 23:10:37 UTC = 2026-08-27 07:10:37 +08 |
| static gates | `verify_questions.py F4` **PASS** (333 items, reachable 333/333, dead-conditions 0, bad-skips 0); `stamp_version.py show` exit 0, no pff/qsf drift |
| duplicate-label gate | `apply_aug21.py --only F4 --fail-on-pre` **0 violations on a row this apply writes, 0 un-ruled pre-existing sets**, exit 0 — the STRICT path, and nothing is ruled benign in `duplicate_label_accepted.json` |
| wave gates | `run_aug21_gates.ps1 -Inst F4 -PreBridge 0` **GATES CLEAN**, exit 0 — scan total 1482 → 1482, no reason grew; bridge B/C markers 0 vs pre 0 |
| replay | post-apply dry run writes **0 / replaces 0 / removes 0** in all seven locales |
| compile | fresh-Designer **Compile Successful at 07:07:52** (`automation/shots/F4_compile.png`) |
| byte-verify | **`RESULT: ALL PASS`**, exit 0 — `v3.2.3` present; seven `0x` counts — `v3.2.2`, `v3.2.1` and five strings only v3.2.2 carried; `Balaud`, `Pagtrabaho ha gobyerno`, `Mahirap magparehistro`, `Awan ti oras nga agparehistro` and `Mababayaran han PhilHealth an gastos han pagtambal` occur **1×** each — the pooled `.pen` string table holds one copy, so the count proves presence, and `dcf-removal-proof-3.2.3.txt` proves the per-code assignment |
| device check | the served `.pen` (md5 `2533a62a2ec1efe1f14638ea5395a014`) is on the `capi_tablet` AVD byte-identical to the one inside the served zip, its `.pff` reads `Household Survey (F4) - v3.2.3 (2026-08-27) [DEV]`, and the app list shows that string (`00-app-list-f4-3.2.3.png`) |

The PSA submission set is untouched and stays frozen at tag `capi-psa-2026-08-20`
(F1 v3.1.5 / F2 v3.0.0 / F3 v6.0.2 / F4 v3.1.3). Everything in this wave is DEV channel.

## Evidence (SHA-pinned, commit `507406e5a64654110f5760cc0796216e10ceb2a3`)

Base: `https://raw.githubusercontent.com/cplreyes/ASPSI-DOH-UHC-CAPI-Development/507406e5a64654110f5760cc0796216e10ceb2a3/docs/uat-fix-evidence/2026-08-26-aug21-translations/F4/`

| file | what it shows |
|---|---|
| `00-app-list-f4-3.2.3.png` | CSEntry app list carrying the `v3.2.3 (2026-08-27) [DEV]` stamp |
| `byte-verify-3.2.3.txt` | the served package verified byte-wise (`RESULT: ALL PASS`) |
| `dcf-removal-proof-3.2.3.txt` | every removed row renders the ENGLISH label, every kept sibling still carries its translation, and no value set in any of the seven languages has two codes with the same label |
| `f4_q2_1_age_fil.png` | Q2.1 + the Q3 option list in Filipino on **v3.2.2** — the on-screen proof that the brackets are gone |
| `f4_q2_1_age_ceb.png` | the same frame in Cebuano (no brackets) |
| `f4_q2_1_age_en.png` | the same frame in English, as the baseline |
| `00-app-list-f4-3.2.2.png` | CSEntry app list carrying the superseded `v3.2.2 (2026-08-26) [DEV]` stamp |
| `README.md` | capture method, substitutions, and the resolved bracket defect in full |

Two caveats the evidence README states and this note repeats: all four deploy captures
(`00-deploy-result.png`, `-3.2.1.png`, `-3.2.2.png`, `-3.2.3.png`) are byte-identical (md5
`4fc57525a8d70801955919f0d924d2dc`) because CSDeploy's dialog leaves `Description` empty —
they prove *a* deploy succeeded and nothing about which version. What proves v3.2.3 is the
app-list shot plus the byte-verify and the built-dictionary proof.
And the shots are Tier-2, on the `capi_tablet` AVD: there is no physical tablet. The
throwaway case `010280001901` was never synced and must not be.

The Q40 and Q67 screens the plan originally named could not be shot: neither has a FIL or
CEB map value (both fall back to English), so a screenshot of them would have proved nothing
about the import. `Q2_1_AGE` (wave-changed in both locales) and the `Q3_SEX` option list
(wave-changed in FIL) were substituted.
