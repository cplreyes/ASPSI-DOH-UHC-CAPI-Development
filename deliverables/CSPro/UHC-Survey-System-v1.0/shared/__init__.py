"""Shared helpers for the UHC Survey System v1.0 CAPI rebuild.

Modules:
  - case_id        — 12-digit RR-PP-MMM-FF-CCC ID-item block builder
  - psgc           — PSGC value-set loaders (PSA 1Q 2026)
  - gps_photo      — auto-GPS + verification-photo block builder
  - value_sets     — common value sets (NA codes, yes/no, sex, etc.)
  - cspro_helpers  — DCF/FMF/APC writer primitives (Phase 1+)
"""
