# Production build env for the F2 PWA (loaded by `vite build` only — NOT by vitest,
# which runs in test mode, so the full multi-language test suite stays green).
#
# 2026-06-27 (#774/#768): the TEMPORARY English-only screenshot mode (VITE_ENGLISH_ONLY,
# Shan request 2026-06-16) is REVERTED at ASPSI's request — Shan re-opened the translations
# ask (#774). Removing the flag re-expands READY_LOCALES to English + the 7 PSA-target
# languages, shows the language switcher, and renders the delivered translations (untranslated
# strings still fall back to English). No active prod-only vars remain; CI redeploys on push.
