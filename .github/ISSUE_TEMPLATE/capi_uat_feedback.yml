name: CAPI UAT Feedback (F1 / F3 / F4 / Hub)
description: Report something you saw while testing the CSEntry CAPI surveys or the Supervisor & Enumerator Hub (Round 6)
title: "[UAT R6][__] "
labels: ["uat-feedback", "from-uat-round-6-2026-06"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for testing! Fill in what you observed — **no technical knowledge needed**, just describe what you saw. One form per finding. A screenshot or screen recording of the screen helps a lot.

        💬 For quick discussion, also post in the round's Slack channel.

  - type: dropdown
    id: instrument
    attributes:
      label: What were you testing?
      options:
        - Hub — Supervisor & Enumerator app (login / menu / Bluetooth / relay / reports / map)
        - F1 — Facility Head Survey
        - F3 — Patient Survey
        - F4 — Household Survey
    validations:
      required: true

  - type: input
    id: tester
    attributes:
      label: Your name or initials
      placeholder: e.g. MRD or Maria Dela Cruz
    validations:
      required: true

  - type: input
    id: test_date
    attributes:
      label: Date of testing
      placeholder: e.g. 2026-06-15
    validations:
      required: true

  - type: dropdown
    id: scenario
    attributes:
      label: Which test were you doing? (from the Tester Guide)
      options:
        - "Hub: Sign in & role menu (5A)"
        - "Hub: Conduct F1/F3/F4 from the menu (5B)"
        - "Hub: Assign / Receive Enumeration Area — Bluetooth pair (5C)"
        - "Hub: Send / Collect interviews + Relay to CSWeb — Bluetooth pair (5D)"
        - "Hub: Reports & offline EA map (5E)"
        - TC-1 Completed interview
        - TC-2 Refused / Withdrawn (no photo expected)
        - TC-3 Partial save + resume
        - TC-4 "Other (specify)" behaviour
        - TC-5 "Select all that apply" + exclusive-option warning
        - TC-6 Patient-type routing (F3 only)
        - Household roster — repeating members (F4 only)
        - Install / download the app
        - Sync to the server
        - GPS / Camera / Verification photo
        - Combined screens / date picker / drop-downs / auto-advance
        - General observation
    validations:
      required: true

  - type: dropdown
    id: result
    attributes:
      label: Test result
      options:
        - Pass — worked exactly as described
        - Pass with comment — worked but I noticed something
        - Fail — did not work as described
        - Blocked — could not complete the test
    validations:
      required: true

  - type: textarea
    id: observation
    attributes:
      label: What did you observe?
      placeholder: Describe what happened in plain language. If there was an error message, type it here exactly. No need to be technical.
    validations:
      required: true

  - type: input
    id: questionnaire_number
    attributes:
      label: Questionnaire Number of the test case (if any)
      placeholder: e.g. 010280001101
    validations:
      required: false

  - type: input
    id: device
    attributes:
      label: Phone/tablet model + Android version (if you know it)
      placeholder: e.g. Samsung A14, Android 14
    validations:
      required: false

  - type: textarea
    id: screenshots
    attributes:
      label: Screenshot or photo of the screen (optional but very helpful)
      placeholder: Drag and drop an image here, or describe what the screen showed.
    validations:
      required: false
